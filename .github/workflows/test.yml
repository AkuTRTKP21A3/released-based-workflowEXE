name: My first workflow
on: [push]

jobs:
  run_command:
    runs-on: ubuntu-latest
    steps:
      - name: run echo command
        run: echo "Moro muailma"
      - name: run multiple commands
        run: |
         echo "Moro muailma"
         echo "Tämä on Aku"